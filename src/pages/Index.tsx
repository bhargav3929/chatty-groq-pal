import { ChatBot } from "@/components/ChatBot";

const Index = () => {
  return (
    <div className="h-screen w-full bg-gradient-to-br from-background to-background/95">
      <ChatBot />
    </div>
  );
};

export default Index;
